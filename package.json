{
  "name": "take-note",
  "private": true,
  "workspaces": [
    "*"
  ],
  "scripts": {
    "client": "yarn workspace client",
    "postinstall": "yarn install && yarn client run build"
  }, 
  "engines": {
    "node": "10.16.0",
    "yarn": "1.15.2"
  }
}